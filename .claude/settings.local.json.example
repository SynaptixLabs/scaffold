{
  "//": "Copy to settings.local.json (gitignored) for personal Claude Code overrides — permissions, MCP servers, model. Team-shared settings go in settings.json.",
  "permissions": {
    "allow": [],
    "deny": []
  }
}
