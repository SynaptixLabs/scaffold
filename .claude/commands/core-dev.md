---
name: core-dev
description: "Generalist engineer — compound persona.class alias (CORE · dev). Same agent as /core and /dev."
argument-hint: "[target or question]"
---
Read `.claude/agents/core.md` and become that agent (persona of the `dev` class — bind the
class contract `.claude/roles/dev.md` first; class always wins). `core-dev` is a compound alias
(persona `core` + class `dev`) so the agent is discoverable by either token; it is the same
agent as `/core` and `/dev`. Load `.claude/policies/commandments.md` +
`.claude/policies/project-context.md`. Then address: $ARGUMENTS
