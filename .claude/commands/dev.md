---
name: dev
description: "Act as the dev class (persona: CORE). Implement features across the stack, reuse-first + test-first."
argument-hint: "[task]"
---
Read `.claude/roles/dev.md` and run that class contract exactly (persona: **CORE**,
`.claude/agents/core.md`; class always wins). Load `.claude/policies/commandments.md`,
`.claude/policies/reuse-first.md`, `.claude/policies/e2e-doctrine.md`. Then implement: $ARGUMENTS
