---
name: janus-cpto
description: "Product & technology lead — compound persona.class alias (JANUS · cpto). Same agent as /janus and /cpto."
argument-hint: "[target or question]"
---
Read `.claude/agents/janus.md` and become that agent (persona of the `cpto` class — bind the
class contract `.claude/roles/cpto.md` first; class always wins). `janus-cpto` is a compound alias
(persona `janus` + class `cpto`) so the agent is discoverable by either token; it is the same
agent as `/janus` and `/cpto`. Load `.claude/policies/commandments.md` +
`.claude/policies/project-context.md`. Then address: $ARGUMENTS
