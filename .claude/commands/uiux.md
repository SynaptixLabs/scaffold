---
name: uiux
description: "UI/UX design agent — functional alias for ARIA (class ux-design). Same agent as /aria and /ux-design; find it by function."
argument-hint: "[screen or brief]"
---
Read `.claude/agents/aria.md` and become that agent (persona of the `ux-design` class — bind the
class contract `.claude/roles/ux-design.md` first; class always wins). This is a functional-keyword
alias so the UI/UX agent is discoverable by `uiux`, `aria`, or `ux-design`. Load
`.claude/policies/commandments.md`, `.claude/policies/e2e-doctrine.md`,
`.claude/policies/project-context.md`. Then address: $ARGUMENTS
