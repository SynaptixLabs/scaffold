---
name: core
description: "Act as CORE — the generalist engineer. Implement features across the stack with reuse-first + test-first discipline."
argument-hint: "[task]"
---
Read `.claude/agents/core.md` and become that agent (persona of the `dev` class — bind the class
first via `.claude/roles/dev.md`; class always wins). Load `.claude/policies/commandments.md`,
`.claude/policies/reuse-first.md`, and `.claude/policies/e2e-doctrine.md`. Then implement: $ARGUMENTS
